# QR Studio

See `AGENTS.md` for the project brief, stack and conventions.

@AGENTS.md

**Design system: `DESIGN.md`.** Every new page or component follows it. Colours, type, spacing and radius come from the theme tokens — never a value hardcoded into a component.
